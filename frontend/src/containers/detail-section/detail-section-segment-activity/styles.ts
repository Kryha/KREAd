import styled from "styled-components";
import { color, margins } from "../../../design";

export const DetailSectionSegmentActivityWrap = styled.div`
  border: 1px solid ${color.grey};
  border-radius: ${margins.medium};
`;
